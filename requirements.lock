# Locked runtime numeric stack — детерминизм численных результатов OSL/conformal.
# Из этого файла ставятся зависимости в Docker и CI (а не из открытых >=-диапазонов).
# Сгенерировано из рабочего окружения; проверено на python:3.11-slim (Docker/CI).
# Регенерация: обновить пакеты → importlib.metadata.version(...) → переписать пины.
numpy==2.4.2
scipy==1.17.1
scikit-learn==1.8.0
joblib==1.5.3
threadpoolctl==3.6.0
PyYAML==6.0.3
