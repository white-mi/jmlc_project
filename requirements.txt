# Рантайм-ядро Макро-радара (TF-IDF-режим, без сети) — то же, что ставит CI/Docker.
# Полный набор (RAG e5-embeddings, LLM-агенты, LangChain) — через extras в _tools/pyproject.toml:
#   cd _tools && pip install -e ".[agent]"   # LangChain + Anthropic
#   cd _tools && pip install -e ".[eda]"     # pandas/matplotlib/jupyter (DS EDA)
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
pyyaml>=6.0
